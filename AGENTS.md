# 기록 작성 기준

이 저장소는 Alcedo의 보안·홈랩·서버 학습과 작업을 기록하는 Hugo 기술 블로그다. 글을 작성하거나 갱신할 때 `docs/writing-guide.md`를 따른다.

## Hugo 원문을 먼저 작성한다

- 사용자가 제공한 공부, 설계, 구축, 설정 변경, 운영, 장애 해결, 실험 결과를 먼저 `content/notes/` 또는 `content/projects/`에 기술적으로 정리한다.
- 단일 개념·절차·장애는 노트, 여러 단계의 설계·구현·검증이 연결된 작업은 프로젝트로 기록한다. 같은 작업의 후속 변경은 기존 글을 먼저 갱신한다.
- 목적, 환경과 버전, 선택 이유, 실제 절차, 검증 결과, 한계와 복구 방법을 남긴다. 작은 작업은 이 항목을 짧게 합쳐도 된다.
- 대화나 작업 결과에 없는 장비 구성, 명령 실행, 장애, 성과, 수치를 만들지 않는다. 제안한 절차와 실제 실행한 절차를 구분하고, 확인하지 못한 내용은 `미확인` 또는 `미검증`으로 표시한다.
- 민감한 값과 개인 정보는 공개 가능한 예시로 바꾼다. `draft = true`와 `stories/`는 Pages 노출을 막을 뿐, 공개 GitHub 저장소에서 파일을 숨기지 않는다.

## Velog는 Hugo 원문에서 각색한다

- Hugo 원문을 완성한 다음 같은 주제의 `stories/<slug>.md`를 작성한다. 기본 틀은 `docs/templates/velog-story.md`를 사용한다.
- 말투는 재미있고 편하게, 사실·환경·결과·한계는 원문과 같게 유지한다. 비유를 쓰되 실제로 겪지 않은 일을 경험담으로 꾸미지 않는다.
- 기술 원리를 그 글만 읽어도 이해할 수 있게 설명하고, 끝에 정확한 Hugo 글 URL을 연결한다.
- `scripts/export-velog.py`는 링크와 파일 형식만 변환한다. 각색이나 Velog 게시는 수행하지 않는다.
- Velog에 실제 게시된 뒤에만 Hugo front matter의 `storyURL`을 채운다. URL을 추측하지 않는다.
- 사실 수정은 Hugo부터 반영하고 연결된 Velog 초안에도 반영한다. 이미 게시된 Velog까지 수정했다고 주장하려면 실제 반영을 확인한다.

## 검증

Hugo 발행 전 `make check`를 실행한다. 초안 생성·각색 파일 준비·Hugo 배포·Velog 게시의 완료 상태를 구분해서 보고한다. 관련 작업 내용이 아직 제공되지 않았다면 임의의 실습 글을 만들어 채우지 않는다.
